apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: frontend
spec:
  type: ExternalName
  externalName: backend-svc.backend.svc.cluster.local
  ports:
    - port: 5000
      targetPort: 5000